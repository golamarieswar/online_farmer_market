# Frontend

React + Vite frontend for Online Farmer Market.

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` in `.env` if the backend is not running at `http://localhost:5000`.
